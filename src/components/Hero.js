import Image from "next/image";

export default function Hero(){

    return(
        <section  className="min-h-full w-full bg-cover bg-no-repeat flex justify-center items-center">
            <div >
                test
            </div>
        </section>

    );

}